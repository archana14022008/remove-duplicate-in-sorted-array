#include <stdio.h>
#include <stdlib.h>

/**
 * Return an array of arrays of size *returnSize.
 * The sizes of the arrays are returned as *returnColumnSizes array.
 * Note: Both returned array and *columnSizes array must be highly managed by the caller.
 */
int** generate(int numRows, int* returnSize, int** returnColumnSizes) {
    // Set the total number of rows to return
    *returnSize = numRows;
    
    // Allocate memory for the row pointers
    int** triangle = (int**)malloc(numRows * sizeof(int*));
    // Allocate memory to store the size of each row
    *returnColumnSizes = (int*)malloc(numRows * sizeof(int));
    
    for (int i = 0; i < numRows; i++) {
        // Row 'i' has exactly 'i + 1' elements
        (*returnColumnSizes)[i] = i + 1;
        triangle[i] = (int*)malloc((i + 1) * sizeof(int));
        
        // The first and last elements of every row are always 1
        triangle[i][0] = 1;
        triangle[i][i] = 1;
        
        // Fill the interior elements of the row
        for (int j = 1; j < i; j++) {
            triangle[i][j] = triangle[i - 1][j - 1] + triangle[i - 1][j];
        }
    }
    
    return triangle;
}
